<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LineBase">
        <item name="android:background">@color/colorPrimaryDarkNDark</item>
    </style>

    <style name="HorizontalLine" parent="LineBase">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">1px</item>
    </style>

    <style name="VerticalLine" parent="LineBase">
        <item name="android:layout_height">1px</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="ButtonBase">
        <item name="android:gravity">center</item>
        <item name="android:textSize">24dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:clickable">true</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/anime_buttons</item>
    </style>

    <style name="NumberButton" parent="ButtonBase">
        <item name="android:background">@drawable/anime_buttons</item>
    </style>

    <style name="CommandButton" parent="ButtonBase">
        <item name="android:background">@drawable/command_buttons</item>
    </style>

</resources>
